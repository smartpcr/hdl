config:
subcomponents:
  cloud-native:
    subcomponents:
      elasticsearch-fluentd-kibana:
        subcomponents:
          elasticsearch:
            config:
              client:
                resources:
                  limits:
                    memory: "1024Mi"
                  requests:
                    cpu: 25m
                    memory: 256Mi
